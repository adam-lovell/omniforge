rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /vetra_tasks/{document} {
      allow read, write: if true; // ⚠️ Temporary: allows open access for quick testing.
    }
  }
}
